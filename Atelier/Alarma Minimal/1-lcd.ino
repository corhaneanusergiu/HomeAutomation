void ledOn(int pin)
{
    digitalWrite(pin, HIGH);
}

void ledOFF(int pin)
{
    digitalWrite(pin, LOW);
}

void ledBlink(int pin, int on, int off)
{
   unsigned long init = millis();
   if (millis() - init > on, )
}